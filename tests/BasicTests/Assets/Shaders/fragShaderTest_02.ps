

float4 main( float2 uv : TEXCOORD ) : SV_Target {
	float4 col ;
	col.rg = uv ;
	col.ba = uv ;
	return col ;
}

float4 psmain1( float2 uv : TEXCOORD ) : SV_Target {
	float4 col ;
	col.rg = uv ;
	col.ba = col.rg *= uv ;
	return col ;
}
float4 psmain2(  ) : SV_Target {
	float valuez = 0.0f ;
	int number = 0x0000 ;
	for( int i = 0; i < 10; ++i ) {
		valuez *= i ;
	}
	return float4( valuez, -valuez, valuez, -valuez ) ;
}