<Project>
    <!-- Log startup info: -->
    <Target Name="LogBuildStartMessage" BeforeTargets="Build">
        <PropertyGroup>
            <CurrentTime>$([System.DateTime]::Now)</CurrentTime>
            <PlatformNameStr>$(NativePlatformName)</PlatformNameStr>
            <OutputFileExt Condition=" '$(OutputType)' == '' ">$(NativePlatformName)</OutputFileExt>
        </PropertyGroup>
        
        <Message Text="DXSharp SDK build process started @ '$(CurrentTime)' ..." Importance="high" />
        <Message Text="Building :'$(ProjectName)' in: '$(RepoRootPath)'...\n" Importance="high" />
        <Message Text="Assembly :'$(AssemblyName)' Output Target File: '$(OutputDir)$(OutputName)'" Importance="high" />
        <Message Text="SDK Target Frameworks: $(DotNetTargets)" Importance="high" />
        <Message Text="SDK Target Processors: $(PlatformNameStr)" Importance="high" />
        
        <Message Text="Current Target Framework: $(TargetFramework)" Importance="high" />
        <Message Text="Current CPU Architecture: $(TargetCPU)" Importance="high" />
    </Target>
    
    <Target Name="LogDirectXInfoAtStartup" BeforeTargets="Build">
        <Message Text="Agility SDK Build: $(UsingAgilitySDK)" Importance="high" />
        <Message Text="DirectX Platform: '$(NativePlatformName)'" Importance="high" />
    </Target>
    
    
    <Target Name="RunToolsAtStartup" BeforeTargets="Build">
        <PropertyGroup>
            <StartupScriptName>DXSBuildStartup.ps1</StartupScriptName>
        </PropertyGroup>
        
        <Message Text="Running Pwsh Scripts @ '$(PowershellScriptsDir)'" Importance="high" />
        <Exec Command="pwsh.exe -NonInteractive -ExecutionPolicy Unrestricted -File $(PowershellScriptsDir)\$(StartupScriptName) -Argument1 &quot;$(Arg1)&quot; -Argument2 &quot;$(Arg2)&quot;" />
    </Target>

    


    <!-- Log build completion info: -->
    <Target Name="LogBuildCompletionMessage" AfterTargets="Build">
        <Message Text="DXSharp Sample build process completed for: '$(Assembly)'." Importance="high" />
    </Target>

</Project>


<!--

        &lt;!&ndash; Custom Task: Run an External Tool or Process &ndash;&gt;
<Target Name="RunExternalTool" AfterTargets="Build">
<Exec Command="path\to\your\tool.exe arguments" />
</Target>

        &lt;!&ndash; Custom Task: Execute a PowerShell Script &ndash;&gt;
<Target Name="ExecutePowershellScript" AfterTargets="RunExternalTool">
<Exec Command="powershell.exe -NonInteractive -ExecutionPolicy Unrestricted -File path\to\your\script.ps1" />
</Target>-->
